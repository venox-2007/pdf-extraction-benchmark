version: "3.9"

services:
  benchmark:
    build: .
    container_name: pdf-extraction-benchmark
    env_file:
      - .env.example
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
      - ./config:/app/config
